[pytest]
addopts = -s -v --html=reports/report.html --self-contained-html
testpaths = tests
markers =
    smoke:      Fast sanity checks — run first
    regression: Full regression suite
    controller: Tests requiring SSH to controller
    kubectl:    Tests using kubectl
    infra:      OCI VM provisioning tests
    slow:       Tests that take longer than 60s
