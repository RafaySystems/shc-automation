# Core testing
pytest==8.2.0
pytest-html==4.1.1

# SSH
paramiko==3.4.0

# OCI SDK
oci==2.130.0

# Config / utilities
pyyaml==6.0.1
python-dotenv==1.0.1

# kubectl wrapper
# (uses subprocess — no extra install needed)
